# Use this when running the app on a physical device.
# Replace with your computer's LAN IP (same Wi‑Fi as the phone).
# Find it: Windows: ipconfig → "IPv4 Address"; Mac: System Preferences → Network.
EXPO_PUBLIC_API_URL=http://192.168.1.2:8080
